\section{Phase-Wise Recovery Details}\label{app:piecewise_recovery}

The phase-wise calibration check for the UMR-regulated ADWIN benchmark shows
that the same calibration stays in the right operational range when the drift
regime changes discretely.

\input{artifacts/tables/cuberoot_adwin/piecewise_oracle}

\begin{figure}[!htbp]
\centering
\safeincludegraphics[width=0.95\linewidth]{artifacts/figures/cuberoot_adwin/fig_cuberoot_adwin_piecewise}
\caption{Piecewise-drift recovery. The same regulator calibration follows the
phase-dependent oracle window closely enough to preserve the right operational
scale in each regime.}
\label{fig:cuberoot_adwin_piecewise}
\end{figure}
