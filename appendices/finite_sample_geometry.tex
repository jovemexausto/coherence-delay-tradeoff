\section{Finite-Sample and Carrier-Regime Diagnostics}
\label{app:finite_sample_geometry}

This appendix collects the numerical diagnostics that support the carrier side of
the theory. These checks are not substitutes for the theorem statements. Their
role is to show that the minimum-kernel, extended-regime, and operational-regime
interpretations are consistent with the finite-sample behavior of the current
experiment families.

\subsection{Finite-Sample Geometry Check}

For the finite-sample term, we estimate empirical $W_2$ between two i.i.d.
samples from the same distribution using exact assignment in low and moderate
dimension, then fit a log--log slope in $n$. We also probe the non-identically
distributed fixed-span window directly in one dimension by comparing a
triangular-array sample, with one draw from each drifted component, against an
i.i.d. sample from the same window mixture. In that check the within-window span
$\zeta n^H$ is held fixed, so the experiment targets the minimum-kernel carrier
interpretation rather than a large-span regime in which heterogeneity itself can
become dominant. Finally, we check the Sinkhorn proxy at a few fixed values of
$\varepsilon$.

\input{artifacts/tables/gaussian/finite_sample_geometry}

This table supports the carrier interpretation used in the main text. In low
dimension, the raw i.i.d. Wasserstein term is compatible with a root-$n$
carrier, whereas higher-dimensional raw Wasserstein slows down. In the
one-dimensional fixed-span triangular-array check, the recovered slopes stay near
the corresponding i.i.d. mixture baseline, which is consistent with the
minimum-kernel and design-effect reading used in \cref{prop:minimum_kernel}.

\subsection{Extended and Operational Carrier Regimes}

The next table summarizes the broader regimes beyond the minimum kernel. The
extended rows compare triangular and i.i.d. mixture carriers on embedded
low-intrinsic-dimensional support across span. The operational rows compare
fixed-$\varepsilon$ Sinkhorn carriers across regularization values.

\input{artifacts/tables/conceptual/carrier_layers}

These rows should be read as calibration rather than proof. The extended-regime
rows support the claim that low intrinsic dimension preserves a carrier near the
canonical $a=1/2$ regime without a dramatic triangular-versus-benchmark gap. The
operational-regime rows support the claim that fixed-$\varepsilon$ Sinkhorn acts
as a stable carrier in the current synthetic setting, even though the paper does
not claim a general theorem for that regime.
