# 12. Theorem Ladder
Status: active
Category: roadmap
Prev: 11. Future Work and Open Problems
Next: -

Ordered list of theorem work that is still scientifically worth doing.

## Already closed

- abstract upper law;
- optimal temporal-validity horizon;
- exact finite-`n` uniform-window staleness constant;
- tractable 1D proof model with root-`n` finite-sample rate;
- structural Gaussian lower bound;
- Gaussian location minimax benchmark;
- compact Gaussian constants proposition.

These are not the current bottleneck.

## Highest-leverage next theorem moves

### 1. Full Gaussian-scale lower theorem

Move from code-level benchmark evidence to a theorem-level lower result.

### 2. Family-level horizon inheritance theorem

Replace the current regular-family conjecture by a theorem for a useful regular
dominated class.

### 3. Fixed-`epsilon` Sinkhorn horizon inheritance theorem

Replace the current operational conjecture by a full triangular-array theorem.
The next usable target is the calibrated embedded `k=2` moderate-band
inheritance statement on a band bounded away from zero, with the theorem line
organized around uniform self-coupling stability, uniform linearization, and a
root-`n` influence-class bound.
The exact missing sub-lemma is the centered self-coupling stability statement on
`B=[eps_min, eps_max]` with a strict contraction bound on the self blocks and a
uniform inverse-norm bound on the centered subspace.

### 4. Lower theory beyond the root-`n` regime

Develop structural lower arguments matched to finite-sample exponents other than
`1/2`.

## What should stop consuming time

- re-opening the identity;
- treating supporting constants as a second theorem line;
- blurring conjectures into theorem-shaped prose;
- putting repository-scale provenance back into the main text.
