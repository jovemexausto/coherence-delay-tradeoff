[project]
name = "coherence-delay-tradeoff-experiments"
version = "0.1.0"
description = "Experiments and simulations for the Coherence-Delay Trade-off paper"
authors = [{name = "Vinicius Parreira", email = "figueiredo@pm.me"}]
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "pandas",
    "seaborn",
    "river"
]

[tool.uv]
# uv settings can be added here if needed
